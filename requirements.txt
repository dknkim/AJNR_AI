# --- core data ---
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # parquet
requests>=2.31

# --- LLM client (talks to local vLLM OpenAI-compatible server) ---
openai>=1.30

# --- embeddings ---
torch>=2.1
transformers>=4.40
sentence-transformers>=3.0
adapters>=0.2.0        # only needed for SPECTER2 proximity adapter

# --- clustering / topic modeling ---
umap-learn>=0.5.5
hdbscan>=0.8.33
scikit-learn>=1.3

# --- influence networks ---
networkx>=3.1

# --- viz ---
matplotlib>=3.7
seaborn>=0.13

# --- serving (install separately on the GPU host) ---
# vllm>=0.6.0
